import React from 'react';
import tw from 'twin.macro';

const ProductsContainer = () => {
    return (
        <p css={tw`text-green-500`}>Products go here</p>
    );
};

export default ProductsContainer;
